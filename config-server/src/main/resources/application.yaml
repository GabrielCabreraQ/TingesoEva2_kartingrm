server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/GabrielCabreraQ/TingesoEva2_kartingrm
          search-paths: config-data
  application:
    name: config-server
