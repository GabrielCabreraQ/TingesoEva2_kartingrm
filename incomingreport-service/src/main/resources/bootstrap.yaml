spring:
  application:
    name: incomingreport-service
  config:
    import: configserver:http://config-server-service:8888
  cloud:
    config:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
